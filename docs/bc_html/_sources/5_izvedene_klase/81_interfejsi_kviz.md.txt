# Квиз

## Питање 1

```{mchoice}
:answer1: Метода `m()` у класи `C` мора бити јавна, јер су све методе у интерфејсу `I` имплицитно јавне.
:answer2: Тип вредности коју враћа метода `m()` у класи `C` мора одговарати типу методе `m()` у интерфејсу `I`.
:answer3: Назив методе `m()` у класи `C` мора одговарати називу методе `m()` у интерфејсу `I`.
:answer4: Ако у методи `m()` у класи `C` постоје параметри, онда они морају одговарати параметрима методе `m()` у интерфејсу `I`.
:correct: 1,2,3,4

Нека је у интерфејсу `I` дефинисана метода `m()` и нека је у класи `C`
имплементиран интерфејс `I`. Који су од наведених исказа тачни?
```

## Питање 2

```{mchoice}
:answer1: Методу у базној класи треба прогласити надјачаном, а у изведеној класи виртуелном.
:answer2: Методу у базној класи треба прогласити виртуелном, а у изведеној класи надјачаном.
:answer3: Методу у базној класи треба прогласити апстрактном, а у изведеној класи надјачаном.
:answer4: Методу у базној класи треба прогласити јавном, а у изведеној класи приватном.
:correct: 2

Ако је у базној класи нека метода имплементирана интефејсом, а у изведеној
класи треба надјачати ту методу, како се морају прогласити методе?
```

## Питање 3

```{fitb}
:answer: "D,B,I"

Попуни празнине цифрама (не речима).

Ако је `B` базна класа, а `D` класа изведена из класе `B` уз имплементацију
интефејса `I`, како треба да наведеш идентификаторе `B`, `D` и `I` приликом
дефиниције класе `D`?

Попуни празнине великим латиничним словима:

`class` |blank| `:` |blank|`,` |blank|
```

## Питање 4

```{mchoice}
:answer1: Мора бити дефинисана метода `c()`.
:answer2: Морају бити дефинисане методе `b()` и `c()`.
:answer3: Морају бити дефинисане методе `a()`, `b()` и `c()`.
:answer4: Морају бити дефинисане методе `a()` и `c()`.
:correct: 3

Нека је у интерфејсу `IA` дефинисана метода `a()`, у интерфејсу `IB:IA`
метода `b()` и у интефејсу `IC:IB` метода `c()`. Ако је у класи `C`
имплементиран интерфејс `IC`, шта мора бити дефинисано у тој класи?
```

## Питање 5

```{mchoice}
:answer1: Мора бити дефинисана метода `a()`.
:answer2: Мора бити дефинисана метода `b()`.
:answer3: Мора бити дефинисана метода `c()`.
:answer4: Морају бити дефинисане методе `a()`, `b()` и `c()`.
:answer5: Једна класа не може да имплементира више интерфејса јер C# не подржава вишеструко наслеђивање.
:correct: 4

Нека је у интерфејсу `IA` дефинисана метода `a()`, у интерфејсу `IB` метода
`b()` и у интефејсу `IC` метода `c()`. Ако су у класи `C` имплементирани
интерфејси `IA`, `IB` и `IC`, шта мора бити дефинисано у тој класи?
```

## Питање 6

Наведени су кључне речи које одређују типове класа:

1. `abstract`
2. `sealed`
3. `partial`
4. `interface`

```{fitb}
:answer: "3,4,1,2"

У празнине испед описа упиши цифру редног броја под којим је наведен одговарајући тип класе.

|blank| - класа која се простире у више фајлова. |blank| - класа садржи само
декларације метода, али не и дефиницију (тело) методе. |blank| - класа која се
не може инстанцирати. |blank| - класа из које се не може наслеђивати.
```
